# Direct production dependencies. Keep this file aligned with
# ``project.dependencies`` in pyproject.toml.
#
# Lock generation toolchain:
#   CPython 3.11.x
#   pip==24.0
#   pip-tools==7.4.1
#   python -m piptools compile --generate-hashes --resolver=backtracking \
#     --strip-extras --no-emit-index-url --no-emit-trusted-host \
#     --output-file=requirements/production.lock requirements/production.in

cryptography>=42,<47
fastapi>=0.115,<=0.136.1
httpx>=0.27,<0.28
psycopg[binary,pool]>=3.2,<4
pydantic>=2.9,<2.13
starlette>=0.46,<1.1
uvicorn>=0.27,<1
